import { SearchregionsPipe } from './searchregions.pipe';

describe('SearchregionsPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchregionsPipe();
    expect(pipe).toBeTruthy();
  });
});
